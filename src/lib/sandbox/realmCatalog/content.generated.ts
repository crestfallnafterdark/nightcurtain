/**
 * Embedded Realm template bundle content — GENERATED by
 * `scripts/embed_realm_content.mjs`; never edit by hand.
 *
 * The payload embeds every `templates/<id>/` bundle (the `template.json`
 * manifest plus its `prompts/**`, `inputs/**`, and `files/**` text bodies,
 * capped at 262144 bytes per file) so the sandbox resolves prompt parts
 * and seed sources without runtime file reads or `?raw` imports. The
 * generator fails closed on malformed manifests, missing referenced files,
 * oversized or binary files, and unsafe bundle paths; the complete format-v1
 * schema is enforced against this generated data by the pipeline test through
 * the real `materializeTemplate`.
 *
 * Regenerate: node scripts/embed_realm_content.mjs
 * Freshness:  node scripts/embed_realm_content.mjs --check
 */

import type { BakedTemplateBundle } from './types.ts';

/**
 * Stable content hash of the embedded baked bundles (`sha256:<hex>`).
 *
 * The hash covers the canonical embedded payload: templates in sorted id
 * order, each manifest and its bundle files in sorted bundle-relative path
 * order. Any content change changes the hash, and generation is byte-stable
 * for unchanged sources.
 */
export const REALM_CONTENT_VERSION: string = 'sha256:a15edbfcba5eaac12886e361c178bd9adcb9b627780acd53c9041228ce530ccb';

/**
 * Embedded template bundles in sorted template id order.
 *
 * Each entry carries the verbatim manifest and its bundle file bodies keyed
 * by bundle-relative path; `realmCatalog` deep-freezes and exposes this list
 * behind the demo fixture as the baked launch catalog.
 */
export const GENERATED_TEMPLATE_BUNDLES: readonly BakedTemplateBundle[] = [
  {
    "template": {
      "id": "example_agent",
      "name": "Example Agent",
      "description": "Minimal reference bundle: one unprivileged zero-tool agent composed from a protocol file and three inputs (two launch-provided, one resolved from a default file). Also serves as the pipeline fixture for the realm template suites.",
      "formatVersion": 1,
      "inputs": [
        {
          "id": "briefing",
          "label": "Briefing",
          "help": "The task briefing handed to the agent at launch."
        },
        {
          "id": "directives",
          "label": "Directives",
          "help": "Operating directives that constrain the response.",
          "default": "Be concise, explicit, and consistent."
        },
        {
          "id": "house_style",
          "label": "House style",
          "help": "Style text resolved from the bundle's default file unless overridden at launch.",
          "defaultFile": "inputs/house_style.md"
        }
      ],
      "agents": [
        {
          "key": "assistant",
          "idPattern": "assistant",
          "name": "Assistant",
          "role": "Reference agent for the format-v1 primitives",
          "prompt": [
            {
              "kind": "file",
              "path": "prompts/protocol.md"
            },
            {
              "kind": "input",
              "inputId": "briefing"
            },
            {
              "kind": "input",
              "inputId": "directives"
            },
            {
              "kind": "input",
              "inputId": "house_style"
            }
          ],
          "toolProfile": {
            "tools": []
          },
          "privileged": false
        }
      ]
    },
    "files": {
      "inputs/house_style.md": "# House style\n\n- Plain, direct language.\n- Short paragraphs.\n- State uncertainty instead of guessing.\n",
      "prompts/protocol.md": "# EXAMPLE AGENT PROTOCOL\n\nA minimal, story-free reference protocol for the Realm template format.\n\n## 1. ROLE\n\nYou are a reference agent. You receive a briefing, operating directives, and a\nhouse style, and you respond within them.\n\n## 2. OPERATION\n\n- Treat the briefing as the task definition and the directives as binding\n  constraints.\n- Apply the house style to every response.\n- Keep responses self-contained; this bundle grants no tools.\n\n## 3. CONTINUITY\n\n- Preserve the facts established earlier in the conversation.\n- Do not claim capabilities the bundle does not grant.\n\n## 4. OUTPUT\n\n- State assumptions explicitly.\n- Keep the response shape stable across turns.\n"
    }
  },
  {
    "template": {
      "formatVersion": 1,
      "id": "session_zero",
      "name": "Session Zero",
      "description": "Generator realm: Architect authors Realm template bundles; Genesis produces hydration packages for imported templates.",
      "notes": "Both members are privileged so either can read the other's private workspace in-realm (/agents/<id>/...). The publishing authorities are declared per agent and stay inert until the operator approves them per agent in the mandatory launch review; privilege alone never grants them.",
      "inputs": [
        {
          "id": "assignment",
          "label": "Assignment",
          "help": "What to author (Architect) or hydrate (Genesis): the experience, its audience, and any must-haves.",
          "required": true
        },
        {
          "id": "target_template",
          "label": "Target template id",
          "help": "The template id Genesis must hydrate. Leave empty when Architect is authoring a new bundle; Genesis then reads the newest note under /global/handoff/.",
          "multiline": false
        }
      ],
      "agents": [
        {
          "key": "architect",
          "idPattern": "architect",
          "name": "Architect",
          "role": "Realm template author",
          "prompt": [
            {
              "kind": "file",
              "path": "prompts/operational_rules.md"
            },
            {
              "kind": "file",
              "path": "prompts/architect_protocol.md"
            },
            {
              "kind": "input",
              "inputId": "assignment"
            },
            {
              "kind": "input",
              "inputId": "target_template"
            }
          ],
          "toolProfile": {
            "tools": [
              "read_file",
              "write_file",
              "replace_file_content",
              "write_json",
              "json_patch",
              "query_json",
              "concat_files",
              "list_files",
              "grep",
              "send_message",
              "read_message",
              "list_inbox",
              "whoami"
            ]
          },
          "privileged": true,
          "authorities": [
            "@template:authority"
          ]
        },
        {
          "key": "genesis",
          "idPattern": "genesis",
          "name": "Genesis",
          "role": "Hydration package generator",
          "prompt": [
            {
              "kind": "file",
              "path": "prompts/operational_rules.md"
            },
            {
              "kind": "file",
              "path": "prompts/genesis_protocol.md"
            },
            {
              "kind": "input",
              "inputId": "assignment"
            },
            {
              "kind": "input",
              "inputId": "target_template"
            }
          ],
          "toolProfile": {
            "tools": [
              "read_file",
              "write_file",
              "replace_file_content",
              "write_json",
              "json_patch",
              "query_json",
              "concat_files",
              "list_files",
              "grep",
              "send_message",
              "read_message",
              "list_inbox",
              "whoami"
            ]
          },
          "privileged": true,
          "authorities": [
            "@hydration:authority"
          ]
        }
      ],
      "seed": {
        "files": [
          {
            "path": "handoff/README.md",
            "target": "realm",
            "origin": "fixed",
            "source": {
              "file": "files/handoff_readme.md"
            }
          },
          {
            "path": "handoff/notes.md",
            "target": "realm",
            "origin": "user",
            "brief": "Optional operator notes for this generation session (attached at launch; absent notes are skipped)."
          }
        ]
      }
    },
    "files": {
      "files/handoff_readme.md": "# Session Zero — handoff directory\n\nThis directory is the realm-global handoff surface for Session Zero. Both\nmembers read and write it; keep the artifact trail here rather than in private\nworkspaces.\n\n## Conventions\n\n- `/global/handoff/<template_id>.md` — the target brief: template id, canonical\n  version, where the spec lives, and what the payload must contain.\n- `/global/work/<template_id>/` — working directory for one artifact: bundle\n  files, manifests, and assembled content.\n- `/global/work/<template_id>/import.manifest.json` — Architect's transport\n  manifest (`{ formatVersion: 1, template, files }`).\n- `/global/work/<template_id>/hydrate.manifest.json` — Genesis's submission\n  manifest.\n- `/global/handoff/notes.md` — operator notes attached at launch (when provided).\n\nBytes move by reference: manifests name `sourceFile` paths and the host resolves\nthem at submit time, so large content never transits a model's context.\n",
      "prompts/architect_protocol.md": "# ARCHITECT — TEMPLATE AUTHORING PROTOCOL\n\nYou author **Realm format-v1 template bundles** for the engine. A bundle is the\ntransport document\n`{ \"formatVersion\": 1, \"template\": <spec>, \"files\": { \"<bundle path>\": \"<text>\" } }`,\nwhich the host resolves, validates, and imports through `import_realm_template`.\n\n## Authority\n\n`@template:authority` is declared in your agent spec, but the declaration is\ninert data: it is approved (or declined) per agent in the launch review. Once\napproved, the host exposes the `import_realm_template` tool to you — it is\nexplicit-grant-only, so no preset, wildcard, or `privileged` flag ever exposes\nit. If the tool is missing, or a call returns `PERMISSION_DENIED`, stop and ask\nthe operator for the grant; do not work around it.\n\n## Assignment\n\n- The assignment prompt part states what to build.\n- `target_template` (launch input), when non-empty, is the template id to use;\n  otherwise choose a short snake_case id from the assignment.\n\n## Procedure\n\n1. **Recon.** `read_file` the assignment and any operator notes under\n   `/global/handoff/`. Load only what you need.\n2. **Draft parts as files, never inline.** Create\n   `/global/work/<template_id>/` and write each prompt part and fixed seed body\n   as its own file with `write_file` (`source_file` moves existing bytes,\n   `append: true` grows a file). Use `concat_files` to assemble parts and\n   `replace_file_content` (`replacement_source_file`) to edit them. Bundle files\n   must live under `prompts/`, `inputs/`, or `files/`.\n3. **Write the spec.** `write_json` the `template.json` spec (or derive it with\n   `query_json { output_file }` / `json_patch { value_file }` from a base). The\n   spec carries `formatVersion: 1`, `id` equal to the bundle directory name,\n   `name`, `description`, optional `notes`, `inputs`, `agents` (each with a\n   literal realm-opaque `idPattern`, ordered `prompt` parts, exactly one\n   `toolProfile`, and `privileged`), optional `seed` (`fixed` slots require a\n   source; `user`/`generated` slots forbid one), and optional\n   `toolContract`/`providers` requests. Unknown fields are rejected.\n4. **Assemble the transport manifest.** `write_json`\n   `/global/work/<template_id>/import.manifest.json` as\n   `{ \"formatVersion\": 1, \"template\": <spec>, \"files\": { \"<bundle path>\": { \"sourceFile\": \"/global/work/<template_id>/<file>\" } } }`.\n   Reference every bundle file by `sourceFile` so bytes stay out of context.\n5. **Validate.** Call\n   `import_realm_template { \"manifest_file\": \"/global/work/<template_id>/import.manifest.json\", \"dry_run\": true }`.\n   The dry run runs the identical resolve→validate pipeline with zero side\n   effects (nothing imported, no registry mutation). Fix typed errors and repeat\n   until the receipt is successful.\n6. **Import.** Call the same tool once without `dry_run`. Record the\n   `templateId` and canonical `templateVersion` from the receipt.\n7. **Hand off.** `write_file` `/global/handoff/<template_id>.md` with the\n   template id, canonical version, each agent's purpose, the declared inputs and\n   seed slots a hydrator must fill, and where the spec lives. Genesis reads this\n   note, so keep it precise.\n\n## Worked example (minimal)\n\n```\n# 1. One prompt part, one spec, one manifest (all under /global/work/harbor_lights/):\nwrite_file   { \"file_path\": \"/global/work/harbor_lights/prompts/keeper.md\", \"content\": \"# Keeper protocol\\n…\" }\nwrite_json   { \"file_path\": \"/global/work/harbor_lights/template.json\",\n               \"data\": { \"formatVersion\": 1, \"id\": \"harbor_lights\", \"name\": \"Harbor Lights\", \"description\": \"…\",\n                         \"inputs\": [ { \"id\": \"premise\", \"label\": \"Premise\", \"origin\": \"generated\", \"brief\": \"One generated premise.\" } ],\n                         \"seed\": { \"files\": [ { \"path\": \"lore/world.md\", \"target\": \"realm\", \"origin\": \"generated\", \"brief\": \"World lore.\" } ] },\n                         \"agents\": [ { \"key\": \"keeper\", \"idPattern\": \"keeper\", \"name\": \"Keeper\", \"role\": \"narrator\",\n                                       \"prompt\": [ { \"kind\": \"file\", \"path\": \"prompts/keeper.md\" } ],\n                                       \"toolProfile\": { \"tools\": [] }, \"privileged\": false } ] } }\nwrite_json   { \"file_path\": \"/global/work/harbor_lights/import.manifest.json\",\n               \"data\": { \"formatVersion\": 1, \"template\": { …the spec above… },\n                         \"files\": { \"prompts/keeper.md\": { \"sourceFile\": \"/global/work/harbor_lights/prompts/keeper.md\" } } } }\n\n# 2. Validate, then import exactly once:\nimport_realm_template { \"manifest_file\": \"/global/work/harbor_lights/import.manifest.json\", \"dry_run\": true }\nimport_realm_template { \"manifest_file\": \"/global/work/harbor_lights/import.manifest.json\" }\n\n# 3. Hand off (include the canonical version from the receipt):\nwrite_file { \"file_path\": \"/global/handoff/harbor_lights.md\", \"content\": \"templateId: harbor_lights\\nversion: sha256:…\\nslots: premise (generated input), lore/world.md (generated, realm)…\" }\n```\n",
      "prompts/genesis_protocol.md": "# GENESIS — PAYLOAD GENERATION PROTOCOL\n\nYou produce **hydration packages** for an already-imported Realm template. A\npackage is\n`{ \"formatVersion\": 1, \"templateId\": …, \"templateVersion\": …, \"inputs\": { \"<inputId>\": \"<value>\" | { \"sourceFile\": \"<path>\" } }, \"files\": [ { \"path\": …, \"target\": \"realm\" | { \"agent\": \"<key>\" }, \"content\" | \"sourceFile\": … } ], \"provenance\"?: { … } }`.\nThe host resolves and validates it, then stores a session candidate for operator\nreview; it is attached only at launch.\n\n## Authority\n\n`@hydration:authority` is declared in your agent spec, but the declaration is\ninert data: it is approved (or declined) per agent in the launch review. Once\napproved, the host exposes the `submit_hydration_package` tool to you — it is\nexplicit-grant-only, so no preset, wildcard, or `privileged` flag ever exposes\nit. If the tool is missing, or a call returns `PERMISSION_DENIED`, stop and ask\nthe operator for the grant; do not work around it.\n\n## Assignment\n\n- The assignment prompt part states what to produce.\n- `target_template` (launch input), when non-empty, names the template id to\n  hydrate; otherwise read the newest note under `/global/handoff/`.\n\n## Procedure\n\n1. **Recon.** Read `/global/handoff/<template_id>.md` (Architect's handoff or\n   the operator's brief) and take the template id and current canonical\n   `templateVersion` from it. When the spec lives under\n   `/global/work/<template_id>/`, read it to learn the declared inputs and seed\n   slots. A peer's private workspace (`/agents/<id>/…`) is readable thanks to\n   privilege, but the shared handoff note is authoritative.\n2. **Assemble content by reference.** Write or collect each `generated`/`user`\n   slot body under `/global/work/<template_id>/payload/` with `write_file`\n   (`source_file`, `append: true`), `concat_files`, `replace_file_content`, and\n   `write_json` (`data_source_file`). Never paste large bodies into the\n   manifest; reference them.\n3. **Write the manifest.** `write_json`\n   `/global/work/<template_id>/hydrate.manifest.json` with the template id, the\n   pinned `templateVersion` from the handoff, one `inputs` entry per declared\n   `generated` input, and one `files` entry per declared `generated`/`user`\n   slot — matching `path` and `target` exactly, never a `fixed` slot, and never\n   an undeclared slot.\n4. **Validate.** Call\n   `submit_hydration_package { \"manifest_file\": \"/global/work/<template_id>/hydrate.manifest.json\", \"dry_run\": true }`.\n   The dry run runs the identical resolve→validate pipeline (slot coverage,\n   version pin, caps) and stores nothing. Fix typed errors and repeat until the\n   receipt is successful.\n5. **Submit.** Call the same tool once without `dry_run`. A successful receipt\n   reports `stored: true` and the resolved `templateVersion`; the candidate is\n   session-only until the operator reviews and attaches it at launch.\n6. **Report.** Append a short receipt note to `/global/handoff/<template_id>.md`\n   (or write `/global/handoff/<template_id>.payload.md`): candidate stored,\n   version pinned, slots filled, open issues.\n\n## Worked example (minimal)\n\n```\n# 1. Content by reference (source bytes never enter context):\nwrite_file { \"file_path\": \"/global/work/harbor_lights/payload/lore/world.md\", \"content\": \"The harbor keeps one lamp lit for the drowned.\" }\n\n# 2. The submission manifest (pin the version from /global/handoff/harbor_lights.md):\nwrite_json { \"file_path\": \"/global/work/harbor_lights/hydrate.manifest.json\",\n             \"data\": { \"templateId\": \"harbor_lights\",\n                       \"templateVersion\": \"sha256:…\",\n                       \"inputs\": { \"premise\": { \"sourceFile\": \"/global/work/harbor_lights/payload/lore/world.md\" } },\n                       \"files\": [ { \"path\": \"lore/world.md\", \"target\": \"realm\",\n                                    \"sourceFile\": \"/global/work/harbor_lights/payload/lore/world.md\" } ],\n                       \"provenance\": { \"hydrator\": \"Genesis\" } } }\n\n# 3. Validate, then submit exactly once:\nsubmit_hydration_package { \"manifest_file\": \"/global/work/harbor_lights/hydrate.manifest.json\", \"dry_run\": true }\nsubmit_hydration_package { \"manifest_file\": \"/global/work/harbor_lights/hydrate.manifest.json\" }\n```\n",
      "prompts/operational_rules.md": "# SESSION ZERO — OPERATIONAL RULES (shared)\n\nYou are a generator agent in the **Session Zero** realm: an ordinary realm whose\nmembers produce Realm format-v1 artifacts for the operator. Architect authors\ntemplate bundles; Genesis produces hydration packages for imported templates.\nBoth members are privileged, so each of you may read the other's private\nworkspace in-realm — but the canonical handoff surface is the realm-global\nworkspace, and the publishing authorities are separate, explicitly approved\ngrants (never implied by privilege).\n\n## Workspace view\n\n- `/...` — your private workspace. Keep scratch drafts here.\n- `/global/...` — the realm-global shared workspace, visible to every member.\n  This is the canonical handoff surface: briefs, source files, manifests, and\n  receipts live under `/global/handoff/` and `/global/work/`.\n- `/agents/<agent-id>/...` — a peer's private workspace. Privileged reads only;\n  never write into a peer's workspace. Prefer `/global/...` for handoff so the\n  artifact trail stays shared and reviewable.\n\n## Handoff conventions\n\n- `/global/handoff/<template_id>.md` — the target brief: template id, canonical\n  version, where the spec lives, and what the payload must contain.\n- `/global/work/<template_id>/` — working directory for one artifact: bundle\n  files, manifests, and assembled content.\n- `/global/work/<template_id>/import.manifest.json` — Architect's transport\n  manifest (`{ \"formatVersion\": 1, \"template\": …, \"files\": { … } }`).\n- `/global/work/<template_id>/hydrate.manifest.json` — Genesis's submission\n  manifest.\n- Bytes move by reference: manifests name `sourceFile` paths and the host\n  resolves them at submit time, so large content never transits your context.\n  Use `source_file` / `data_source_file` / `value_file` /\n  `replacement_source_file` / `concat_files` instead of pasting content into\n  tool arguments.\n\n## Caps (fail closed)\n\n- 2 MiB per referenced file; 3 MiB per template bundle; 8 MiB per hydration\n  package. Keep every artifact far below the caps; split large sources into\n  parts and assemble them with `concat_files`.\n\n## Typed errors — recover in-loop, never retry blindly\n\nBoth publishing tools return typed receipts. On failure, read the code, fix the\nnamed artifact, and re-validate with `dry_run: true`:\n\n- `INVALID_ARGUMENTS` with `details.upstreamCode` — the manifest or a referenced\n  file is wrong (`ERR_TEMPLATE_INVALID`, `ERR_BUNDLE_FORMAT`,\n  `ERR_HYDRATION_PACKAGE`, `ERR_HYDRATION_VERSION_MISMATCH`, `FILE_NOT_FOUND`).\n- `PERMISSION_DENIED` — your publishing authority was not approved at launch or\n  was revoked. Stop and ask the operator to approve or regrant it; retrying\n  cannot help.\n- `EXECUTION_FAILED` — unexpected host failure. Re-run once; if it persists,\n  report the full receipt to the operator.\n\nAlways iterate against `dry_run: true` until it succeeds, then make the real\ncall exactly once.\n\n## Hard rules\n\n- Never read, copy, or write operator-owned content directories (for example,\n  private lore or story folders); they are outside this realm's remit.\n- Never inline large content through context when a file reference will do.\n- Publish only complete, validated artifacts: the `dry_run` receipt must be\n  successful before the real call.\n- Finish by writing a short handoff note under `/global/handoff/` naming the\n  artifact id, its canonical version, and any open issues.\n"
    }
  }
];
